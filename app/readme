打包流程
1：打开根目录gradle.properties文件
2：JENKINS_IS_TEST_SERVER修改为对应值
3：检查JENKINS_TINKER_APK_ID，注意.international结尾为测试服使用，注意包名不能随意更换为其他，如果要修改，需要对应修改app项目文件夹下的build.gradle的getAmapValue方法的常量值
3：注意JENKINS_VERSION_CODE和JENKINS_VERSION_NAME不可轻易改成低版本，否则可能和服务器的通信会因为版本不一致而出错
4：打包


需要注册的账号
1：Umeng账号
2：OpenInstall账号
3：音视频账号
4：阿里云推送账号
5：华为、小米、firebase、meizu推送账号
6：备用接口域名账号
7：高德地图、谷歌地图的账号


需要修改的东西：
1：App类，initUmeng方法，替换"aaaaaaaa"为umeng测试的项目key，替换"bbbbbbbb"为umeng正式的项目key（为了不将测试服和正式服的数据混淆了）
2：AndroidManifest文件，替换"com.openinstall.APP_KEY"为对应的值
3：app项目文件夹下，build.gradle中将getAmapValue方法中的"11001100110011001100110011001100"替换为测试项目的高德Key和正式项目的高德Key
4：将app项目文件夹下的google-services.json替换掉(firebase)
5：将app项目文件夹下的aliyun-emas-services.json替换掉(阿里云推送)
6：AndroidManifest文件，替换"com.google.android.maps.v2.API_KEY"为谷歌开发者账户中对应的值
7：AndroidManifest文件，替换"com.huawei.hms.client.appid"为华为开发者账户中对应的值
8：修改bussiness_bridge下的Constant类的LOGIN_HTTP_HOST值
9：修改message_bridge下的Constant类的LOGIN_HTTP_HOST值
10：修改tools_support下的Constant类的BASE_URL_UPLOAD值和UPLOAD_KEY值
11：修改BackupLoginHostInterceptor类的备用域名文件地址和解密key
12：修改RtcEngineHolder中initEngine方法中对应声网的Key
13：修改PushService.initThirdPush方法中的小米的key和id

注意：打包的key和对应密码等信息，都写在app下的build.gradle文件中
注意：app根目录下的qchat.jks和debug.keystore文件不可替换，不可修改，不可删除，必须用这两个keysotre进行打包，否则服务器通讯将会失败